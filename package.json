{
  "name": "santa-portfolio",
  "version": "1.0.0",
  "description": "My wicked Nuxt.js project",
  "author": "santa-sukitoku",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "nuxt": "^2.0.0"
  },
  "devDependencies": {
    "node-sass": "^4.13.0",
    "push-dir": "^0.4.1",
    "reset-css": "^5.0.1",
    "sass-loader": "^8.0.0"
  }
}